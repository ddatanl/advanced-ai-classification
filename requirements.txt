# Classificatie-training - benodigde Python-packages
# Installeer alles in een keer met:
#     pip install -r requirements.txt
#
# Aanbevolen: gebruik Python 3.10 of nieuwer, bij voorkeur in een
# virtuele omgeving (python -m venv .venv).

# --- Kern: nodig voor alle opdrachten ---
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3

# --- Om de notebooks te openen en uit te voeren ---
jupyterlab>=4.0
# Alternatief voor wie liever de klassieke interface gebruikt:
notebook>=7.0
